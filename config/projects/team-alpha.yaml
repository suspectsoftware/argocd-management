apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: team-alpha
  namespace: argocd
spec:
  description: "Project for team Alpha"
  sourceRepos:
    - '*'
  destinations:
    - namespace: '*'
      name: 'in-cluster'
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
